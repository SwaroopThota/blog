<h1>
    <%= title %>
</h1>
<%- include('./partials/search') %>
    <% if(posts.length> 0){ %>
        <% posts.forEach(post=>{ %>
            <%- include('partials/_display_post',{post:post}) %>
                <% });}else{ %>
                    <p class="m-3">No posts to display.</p>
                    <% } %>